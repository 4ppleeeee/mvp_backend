# TripGuard / mvp_backend Handoff

更新时间：2026-07-30（Asia/Shanghai）

## 当前进度

- 已完成以“输入形式 → 来源识别 → 资源探测 → 能力计划 → 证据处理”为核心的 Ingestion 编排；支持 Bilibili、YouTube、抖音、小红书和小宇宙等来源，并按来源组合字幕、元数据、音频转写、视频下载和关键帧能力。
- 已保留字幕优先、无可用字幕时再走 Whisper 的正常流程；抖音解析包含参考 BiliNote 的一次性 `msToken` 访问步骤，不使用 Cookie、验证码、浏览器自动化或验证绕过。
- 已支持小红书视频识别与视频链路、小宇宙纯音频链路，以及完整视频下载、抽帧和拼图返回；关键帧默认关闭，不改变正常解析流程。
- 已完成知识库准入的 `accept / review / reject` 审核流，保留模型判断和原始证据，地点相关但需要人工确认的内容进入 `review`。
- 管理后台已支持任务阶段、百分比、进度说明和更新时间展示，并为已识别来源展示内置 SVG 平台图标；当前 SVG 为临时手写近似图标，部分平台的形状/品牌细节不够准确，需后续优化。
- 已新增 `/admin/poi` POI 抓取与校审控制台：抓取流程通过腾讯 Location API 选择 `poiId`，提交 Crawlab 任务，读取证据并生成 JSON 初稿，人工确认后调用远端 `attraction/create`；校审流程通过远端 `attraction/batchGet` 分页、`get` 详情和 `update` 保存。
- 已增加 `PoiCrawlRecord` 保存 `crawlTaskId`、腾讯 `poiId`、远端 `attractionId`、草稿和同步状态，保证远端景点与本地抓取证据可以关联。
- 已优化 Docker 构建缓存：系统包和 pip 下载使用 BuildKit cache mount，避免每次重建重复下载依赖；仅重启服务不会重新下载依赖。
- 当前改动已同步到 claw 并完成部署；远端运行数据、数据库、上传文件和环境配置保持不变。健康检查已通过。
- 当前改动已同步到 claw 并完成部署；远端运行数据、数据库、上传文件和环境配置保持不变。健康检查已通过。
- 本轮验证：claw Python 3.12 容器中 POI/后台相关测试 `21 passed`，仓库自身测试（排除 claw 残留的 RAG 测试）`99 passed, 2 warnings`。真实公开视频的完整关键帧端到端下载仍未完成验证。

## 下一步规划

1. 优化管理后台平台图标：改用准确、统一授权的官方/规范 SVG 资源，统一尺寸、描边和不同来源的视觉风格。
2. 在真实 Crawlab 成功任务上验证“生成初稿 → 人工确认 → attraction/create”闭环，并补充远端接口失败后的可重试状态。
3. 完善 POI 校审页的证据联动：按 `poiId` 找回抓取页面、显示来源证据，并对远端 `get` 返回字段做更细的编辑校验。
4. 选择体积较小且公开可访问的视频，在 claw 的 Python 3.12 容器内完成一次真实关键帧端到端测试；不得使用 Cookie、验证码或浏览器自动化绕过限制。
5. 持续根据 `review` 人工审核结果迭代地点/旅行相关准入规则和策略版本，保持模型建议与系统约束分离。
